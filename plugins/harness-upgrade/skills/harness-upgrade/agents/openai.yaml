interface:
  display_name: "Harness Upgrade"
  short_description: "Draft an initial repository harness"
  default_prompt: "Use $harness-upgrade to inspect this repository, run Pass 1 only, ask the minimum clarification questions, and after my answers run Pass 2 to draft the initial harness."
policy:
  allow_implicit_invocation: false
